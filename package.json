{
  "name": "rauskuclaw",
  "version": "0.1.0",
  "private": true,
  "description": "RauskuClaw operator CLI",
  "type": "commonjs",
  "bin": {
    "rauskuclaw": "./bin/rauskuclaw.js"
  },
  "scripts": {
    "test": "node --test cli/test/*.test.js",
    "docs:gen": "node scripts/docs-gen.mjs",
    "milestone:complete:dry": "node scripts/milestone.mjs complete --dry-run",
    "milestone:complete:dry:commit": "node scripts/milestone.mjs complete --dry-run --commit",
    "milestone:complete:dry:push": "node scripts/milestone.mjs complete --dry-run --commit --push",
    "milestone:commit": "node scripts/milestone.mjs log --commit",
    "milestone:complete:commit": "node scripts/milestone.mjs complete --commit",
    "milestone:complete:push": "node scripts/milestone.mjs complete --commit --push"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@z_ai/mcp-server": "^0.1.2",
    "ink": "^6.7.0",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "yaml": "^2.8.2"
  }
}
